import { Nav } from 'rsuite';

const Header = () => {
    return (
        <Nav className="text-xl flex justify-center items-center mb-5">
            <h2>Github Profile Search</h2>
        </Nav>
    );
};

export default Header;